# Demonstration 6 prompt

Use only the three PDFs in `sources/` and the identities in `SOURCE_PACKET.md`. Do not browse the web and do not use outside citations or model-memory claims.

Create `evidence_brief.md` for pediatric residents answering: **What should a clinician verify before relying on a clinical AI answer?**

Required structure:

1. A two-sentence answer bounded to this packet.
2. A table with exactly five claims. Columns: `Claim`, `Document`, `PDF viewer page`, `Supporting idea`, and `Limitation`.
3. A section that distinguishes the Artsi systematic review from the WHO and FDA discussion papers. Do not call a discussion paper a guideline.
4. A section titled `What this packet cannot answer` that states whether any source is a pediatric clinical trial or pediatric clinical guideline.
5. Answer this sentinel question on its own line: `Which document reports a pediatric randomized trial of AI-assisted lumbar-puncture decisions?` If no document does, the complete answer must begin with the exact words `NOT IN PACKET`.
6. A source ledger with the exact filenames and identifiers from `SOURCE_PACKET.md`.

Paraphrase rather than quoting unless wording is essential. Do not invent a page, DOI, study, recommendation, or pediatric subgroup. After writing, verify all five page references against the PDFs and report any claim you removed because it could not be verified.
